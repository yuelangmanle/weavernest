# 安全说明

- AI API Key 只保存在织雀的加密本地存储中，不会写入导出的 APK。
- 生成应用的私人运行时 API Key 由安装者填写，不嵌入共享 APK。
- Web 项目不能访问任意 Android 原生接口；原生功能必须经过能力注册表、项目勾选和系统授权。
- 项目签名密钥只保存在 `.local`，首次导出前必须设置备份密码；不要把密钥上传到 GitHub。
- 请不要在公开 Issue 中提交 API Key、签名密钥、个人数据或完整私有项目。

发现安全问题请通过私下渠道联系维护者，不要先公开利用细节。
